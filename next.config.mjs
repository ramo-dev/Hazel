/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['images.unsplash.com', "https://www.behance.net", "firebasestorage.googleapis.com"],
  },
};

export default nextConfig;
